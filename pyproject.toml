[tool.ruff]
target-version = "py313"
line-length = 110
exclude = [".venv", "node_modules", ".runtime", "static/vendor"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"**/migrations/*.py" = ["I"]
